PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/role_based_auth_demo
JWT_SECRET=replace_with_a_strong_secret
JWT_EXPIRES_IN=1d
ADMIN_SECRET=admin2025
NODE_ENV=development

# Optional SMTP (for real email); if empty, reset-link will be logged
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=no-reply@example.com
